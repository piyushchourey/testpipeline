version: 0.2

phases:
  install:
    commands:
      - npm install -g @angular/cli
      - npm install
  build:
    commands:
      - npm run build --prod
      
artifacts:
    files:
        - '/'
    discard-paths: yes
    base-directory: 'dist*'